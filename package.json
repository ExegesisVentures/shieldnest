{
  "name": "shieldnest",
  "version": "1.0.0",
  "description": "ShieldNest - A comprehensive DeFi platform",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "lint": "cd apps/web && npm run lint",
    "clean": "cd apps/web && npm run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
